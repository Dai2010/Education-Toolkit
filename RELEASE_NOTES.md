# Education Toolkit v1.0.0 发布说明

## 🎉 首个正式版本发布

Education Toolkit 是一个专为教育场景设计的桌面工具包，整合了课堂管理中最常用的功能。

---

## ✨ 核心功能

### 📱 桌面时钟
- 保留 Elegant Clock 的核心时钟体验
- 启动后1秒自动进入紧凑模式（可拖动圆角卡片）
- 实时显示当前课程和课间状态
- 完整周课表视图（网格布局，一眼看清整周安排）
- 当前课程高亮显示（上课中/课间双高亮）
- 今天列高亮，快速定位
- 字体、颜色、透明度完全自定义
- 倒计时与提醒功能

### 🎲 随机抽人
- **单人模式**：随机抽取一位同学
- **分组模式**：可设置每组人数和组数，结果按组显示
- **连续不重复抽取**：记录已抽成员，避免重复
- 实时显示已抽人数和剩余人数
- 名单预览（已抽取成员灰色标记）
- 结果字号可调（16-72px）
- 支持 JSON 格式批量导入名单

### 📝 作业布置
- 集中记录作业名称、科目、上交时间
- 关联课表自动计算提醒时间
- 到点发送系统通知
- 桌面作业小组件（置顶显示，可展开/收缩）
- 小组件支持拖动移动和位置记忆
- 作业列表按上交时间排序
- 支持滚动显示（小窗口友好）
- 标记完成/删除/编辑

### ⚙️ 完整设置
- 主题颜色自定义
- 默认铃声设置
- 开机自启动
- 名单管理（添加、删除、分组、导入 JSON）
- 课表管理（按星期查看、编辑、导入 JSON）
- 科目老师统一管理
- 内置帮助文档

---

## 🌟 亮点特性

### 🕐 分时段问候语
主页问候语根据时间自动变化，贴合学生作息：
- **早上**（5:00-8:59）：元气满满开始新一天
- **上午**（9:00-11:59）：上课学习进行中
- **中午**（12:00-13:59）：午餐和午休时间
- **下午**（14:00-17:59）：坚持到放学
- **傍晚**（18:00-18:59）：晚饭时间
- **晚自习**（19:00-21:29）：专心完成作业
- **晚上**（21:30-22:59）：准备休息
- **深夜**（23:00-4:59）：劝你早点睡觉

### 📊 完整周课表
- 传统网格布局，横向7天纵向排列时间段
- 自动提取所有上课时间并排序
- 不显示课间时长，界面更简洁
- 当前课程深紫色渐变高亮
- 课间时前后两节课同时高亮
- 白色文字保证高对比度（WCAG AA 级）
- 响应式设计，移动端友好

### 🔲 智能桌面小组件
- 收缩状态：54×54 紫色方块，显示"✓"图标
- 展开状态：310×190 卡片，显示所有未完成作业
- 内容区域可滚动，支持显示任意数量作业
- 两个按钮（"−"最小化 / "×"关闭）
- 拖动标题栏移动窗口
- 四边边界保护，窗口不会移出屏幕
- 位置自动保存，重启后恢复

### 🎯 连续不重复抽取
- 记录已抽取成员索引
- 剩余人数不足时智能提示
- 一键重置抽取记录
- 名单中已抽成员标记为灰色
- 支持单人和分组两种模式

---

## 🛠️ 技术细节

### 架构设计
- **主应用**：Electron + 原生 JavaScript
- **时钟模块**：集成 Elegant Clock 核心功能
- **数据存储**：JSON 本地持久化
- **独立配置**：主应用和时钟模块配置分离

### 数据格式

#### 名单 JSON 格式
```json
[
  {"name": "张同学", "group": "一组"},
  {"name": "李同学", "group": "二组"}
]
```

或简化格式：
```json
["张同学", "李同学"]
```

#### 课表 JSON 格式
```json
{
  "schedule": [
    {
      "id": "lesson-1",
      "weekday": 1,
      "subject": "数学",
      "course": "数学",
      "start": "08:00",
      "duration": 40,
      "breakDuration": 10
    }
  ],
  "subjectTeachers": {
    "数学": "李老师"
  }
}
```

**字段说明**：
- `weekday`：1-7（周一到周日），省略表示每天重复
- `duration`：课程时长（分钟）
- `breakDuration`：课间时长（分钟）

### 兼容性
- Windows 10/11
- macOS 10.15+
- Linux (主流发行版)

---

## 📦 安装说明

### Windows
下载 `Education-Toolkit-Setup-1.0.0.exe` 并运行安装程序。

### macOS
下载 `Education-Toolkit-1.0.0.dmg`，拖动到 Applications 文件夹。

### Linux
下载 `Education-Toolkit-1.0.0.AppImage` 或 `.deb` 包安装。

---

## 🔄 数据迁移

如果你之前使用过单独的工具（Elegant Clock 或 RollCall），数据已自动独立存储：
- **主应用数据**：`~/.config/Electron/education-toolkit.json`
- **时钟模块数据**：`~/.config/Electron/elegant-clock-state.json`

---

## 🐛 已知问题

- 深夜时段（23:00-4:59）的问候语可能显得有点"啰嗦"（这是特意的，劝你早点睡！）
- 小窗口下作业列表可能需要滚动查看（已优化滚动体验）

---

## 🙏 致谢

感谢所有测试用户的反馈和建议。本项目整合了：
- **Elegant Clock**：桌面时钟核心功能
- **RollCall**：随机抽人核心逻辑

---

## 📄 许可证

本项目以 **GNU General Public License v3.0** 发布。

---

## 📞 反馈与支持

- 项目主页：https://github.com/Dai2010/Education-Toolkit
- 问题反馈：https://github.com/Dai2010/Education-Toolkit/issues

如有问题或建议，欢迎提 Issue！

---

**祝你使用愉快！(＾▽＾)**
